# 世界演变 M3 收口记录

> 版本：A0.1.0-alpha.3  
> 日期：2026-09-26  
> 状态：已完成本地模拟验收

## 已完成

- 新增 `src/世界演变/query/` 查询层；
- MVU 适配器：只读扫描当前/上一份快照，识别新增或变化的命名对象；
- 工作流助手适配器：只读读取楼层任务结果和提取字段；
- ReplicaEnum 适配器：复用工作流助手的严格解析器，仅读取最终枚举块；
- shujuku 适配器：只读扫描导出的表格和行，保留表名、行 ID 和原始行边界；
- 世界演变自身表格适配：正文提及、待办计划参与者、最近事件参与者和后台轮转；
- 手动候选来源；
- 统一去重、来源优先级、证据合并和 NPC/其他对象数量限制；
- 查询上下文注入世界演变 prompt，避免把外部表格压成不可追溯的大字符串；
- 外部输入只读，未写回 MVU、工作流助手或 shujuku；
- 版本更新为 `A0.1.0-alpha.3`，新增对应导入预设。

## 测试

已通过：

- M3 查询层固定输入测试：2/2；
- 世界演变 AI 模拟调用与 prompt 测试：3/3；
- 世界演变数据库适配器测试：2/2；
- 世界演变与数据库相关回归测试：27/27；
- 生产构建：成功；
- 未调用真实 API，未产生网络消耗。

## 下一步

M4 将在 M3 查询上下文之上加入 AI operations schema、本地整批校验、`baseRevision` 冲突检测和模拟提交；M3 查询层本身不负责写数据库。
