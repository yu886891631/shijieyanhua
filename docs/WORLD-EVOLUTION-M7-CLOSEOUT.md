# 世界演变 M7 收口记录

> 版本：A0.1.0-alpha.7  
> 日期：2026-09-26  
> 状态：本地模拟验证完成，SillyTavern 真机未验收

## 本阶段完成

- 面板读取当前聊天的数据库快照和 `worldbook_projection` 账本。
- 世界书目标固定显示当前角色卡 `primary` 世界书，不提供任意书名输入。
- 新增投影检查：
  - 账本投影总数；
  - 已同步、待处理、失败；
  - 缺失、内容漂移、孤儿和旧版托管条目。
- 新增管理操作：
  - 立即同步；
  - 清理孤儿并修复漂移；
  - 完整重建当前聊天投影；
  - 刷新账本；
  - 重试世界书同步。
- 完整重建只删除当前聊天的世界演变托管条目，保留用户条目、其它插件条目和其它聊天的投影。
- 所有操作仍以数据库为事实源，不调用真实 AI。

## 验证

- 世界演变与数据库测试：47/47 通过；
- `pnpm build` 成功；
- `git diff --check` 通过；
- 构建中仍存在工作流助手已有的导出警告，不影响本次世界演变构建；
- 未进行真实聊天、真实 API 或 SillyTavern 真机验收。
