// English-path entry for jsDelivr/Git imports.
// The implementation lives in the Chinese-named source directory to match the
// existing project organization; this wrapper keeps the public import URL simple.
import '../世界演变/index';
